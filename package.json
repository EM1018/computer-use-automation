{
  "name": "legacy-cu-capability-schema",
  "version": "0.1.0",
  "private": true,
  "description": "Artifact schema and loader for the browser-automation capability system",
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "generate:schema": "tsx src/generate-schema.ts",
    "replay": "tsx src/cli/replay.ts"
  },
  "dependencies": {
    "js-yaml": "^4.1.0",
    "zod": "^3.23.8",
    "zod-to-json-schema": "^3.23.5"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^22.10.2",
    "playwright": "^1.63.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^5.0.0"
  }
}
